# Update log — automation-hub — 2026-04-13

## Validation UI: Salesforce skip detail, wording, scrape summary

- **When:** 2026-04-13
- **Area:** `lib/queries.ts`, `components/ValidationPopup.tsx`
- **What:**
  - Select and display **`job_content.scrape_change_summary`** as **Scrape summary** on the email timeline (alongside subject). Filled by `proxi_salesforce_automation` when logging job rows; reflects actual scraped-field changes vs the prior row for that email, not the email subject.
  - **Salesforce field sync skipped** events show **`detail`** from the pipeline (`sf_sync_skipped_no_mapping`, and `sf_scrape_fields_skip` when no field-audit block). Expandable **“Why this was skipped”** panel.
  - **Supabase latest job snapshot** timeline entries for `job_current_sf_ids_changed` (and legacy `job_current_upsert`) with insert vs update wording via `saved_as`.
  - Replaced user-facing **PATCH / upsert** phrasing with **updated in Salesforce**, **insert or update**, **no write sent**, etc.
- **Notes:** Depends on DB migration for `scrape_change_summary` in the automation Python package; see `proxi_salesforce_automation/update_logs/2026-04-13.md`.

---

## Validation query fallback if `scrape_change_summary` column missing

- **When:** 2026-04-13
- **Area:** `lib/queries.ts` — `getValidationData` / `fetchValidationSqlRows`
- **What:** If Postgres errors because `job_content.scrape_change_summary` does not exist yet, retry the same query without that column so Validation Details loads; `scrapeChangeSummary` is then null until migration runs.
- **Why:** Avoid a hard failure of the whole validation API before `ensure_tables` / `migrate_job_tables_extra_columns` has been applied to the connected database.

---

## Validation: event run_id union + Salesforce last in timeline

- **When:** 2026-04-13
- **Area:** `lib/queries.ts`, `components/ValidationPopup.tsx`
- **What:** `events_this_run` matches `jel.run_id` to `jc.run_id`, `es.run_id`, or the opened run id. Timeline sort uses pipeline tie-break so Salesforce field-sync rows (and the synthetic fallback) sort **after** email / Supabase snapshot / mapping when timestamps collide.
- **Why:** Align UI with pipeline order and surface SF audit rows that were filtered out by overly strict `run_id` equality.
